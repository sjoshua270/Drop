<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/comment_prof_image"
        android:layout_width="@dimen/comment_prof_dimen"
        android:layout_height="@dimen/comment_prof_dimen"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_margin="@dimen/small_margin"
        android:contentDescription="@string/prof_img_desc" />

    <TextView
        android:id="@+id/comment_timestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="@dimen/small_margin"
        android:layout_marginRight="@dimen/small_margin"
        android:layout_marginTop="@dimen/small_margin"
        android:textSize="@dimen/item_timestamp_size"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/comment_user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/small_margin"
        android:layout_toEndOf="@id/comment_prof_image"
        android:layout_toLeftOf="@id/comment_timestamp"
        android:layout_toRightOf="@id/comment_prof_image"
        android:layout_toStartOf="@id/comment_timestamp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/comment_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/comment_user_name"
        android:layout_marginBottom="@dimen/smaller_margin"
        android:layout_marginEnd="@dimen/small_margin"
        android:layout_marginRight="@dimen/small_margin"
        android:layout_marginTop="@dimen/smaller_margin"
        android:layout_toEndOf="@id/comment_prof_image"
        android:layout_toRightOf="@id/comment_prof_image" />
</RelativeLayout>